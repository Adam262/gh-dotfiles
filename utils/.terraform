alias tf='terraform'
alias tfi='terraform init -upgrade -backend-config=state.conf'
alias tfim='terraform init -upgrade -backend-config=state.conf -migrate-state'
alias tf-dangerous-a='terraform apply plan.bin && rm -f plan.bin'
alias tfp='rm -f plan.bin && terraform plan -out=plan.bin | tee plan.txt'
